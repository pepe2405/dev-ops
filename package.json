{
  "name": "dev-ops-playground",
  "version": "1.0.0",
  "private": true,
  "description": "Demo Node.js app + secure CI/CD pipeline with GitHub Actions, Snyk, CodeQL, Docker, and Kubernetes",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/<owner>/<repo>.git"
  },
  "bugs": {
    "url": "https://github.com/<owner>/<repo>/issues"
  },
  "homepage": "https://github.com/<owner>/<repo>#readme",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "node --test",
    "audit": "npm audit --audit-level=high",
    "snyk:oss": "snyk test --severity-threshold=high",
    "snyk:code": "snyk code test --severity-threshold=high",
    "snyk:container": "snyk container test dev-ops-playground:local --severity-threshold=high --file=Dockerfile"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.0",
    "eslint-plugin-n": "^17.24.0",
    "eslint-plugin-promise": "^7.2.1",
    "eslint-plugin-security": "^4.0.0",
    "globals": "^17.3.0",
    "prettier": "^3.8.1",
    "supertest": "^7.2.2"
  }
}
