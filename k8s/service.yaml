apiVersion: v1
kind: Service
metadata:
  name: dev-ops-playground
spec:
  selector:
    app: dev-ops-playground
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
